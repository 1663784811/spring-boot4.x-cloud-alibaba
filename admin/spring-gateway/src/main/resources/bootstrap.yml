spring:
  config:
    import: optional:nacos:
  application:
    name: spring-gateway
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        enabled: true
        file-extension: yml
        group: DEFAULT_GROUP