# Bootstrap 配置
spring:
  application:
    name: spring-user
  profiles:
    active: nacos-config,nacos-discovery,log
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        group: DEFAULT_GROUP
      discovery:
        server-addr: localhost:8848