server:
  port: 9002

spring:
  application:
    name: admin-order
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        enabled: true
        file-extension: yml
        group: DEFAULT_GROUP
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        enabled: true
        # 配置服务注册的IP地址
        ip: 192.168.212.38
        port: 9002
